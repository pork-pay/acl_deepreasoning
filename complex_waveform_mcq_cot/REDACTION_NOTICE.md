# Redaction Notice

This complex-waveform-MCQ CoT package was redacted for a **public** repo.
A few internal absolute paths in `selected_complex_cases_2.jsonl` were replaced with placeholders
(`<internal_root>` / `<output_root>` / `<collaborator>` etc.); no CoT text, prompts, options,
gold answers, or figures were changed. `SHA256SUMS.txt` was recomputed over the redacted files
(excludes itself), so integrity still verifies:

```bash
sha256sum -c SHA256SUMS.txt   # all OK
```

